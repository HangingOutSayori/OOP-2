#include <iostream>
#include<windows.h>
#include <fstream>
#include <string>
#include <vector>
#include <algorithm>
#include <regex> // ƒл€ использовани€ регул€рных выражений

using namespace std;
bool isValidRussianPhoneNumber(const string& number);
bool isValidName(const string& name);